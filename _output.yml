bookdown::gitbook:

  number_sections: false

  css: style.css
  
  config:
  
      search:   false        # no search button
      download: false        # no download button
      info:     false        # no info button
      sharing:  false        # no social media button
      toc: 
        collapse: section    # collapse toc at section level